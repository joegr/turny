# Flask Configuration
FLASK_ENV=development
SECRET_KEY=change-me-in-production

# Database
DATABASE_URL=
# HTTPS/Domain Configuration
# Local: Uses localhost with self-signed cert (leave as tournament.local)
# Production: Set to your actual domain for Let's Encrypt
DOMAIN=tournament.local

# Google Cloud (optional - for Pub/Sub)
GCP_PROJECT_ID=
GCP_REGION=us-central1
